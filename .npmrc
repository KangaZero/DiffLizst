# Supply-chain hardening: refuse to install package versions released
# within the last 7 days. Defends against typo-squat / compromised-release
# windows by giving the community time to flag bad versions.
# Value is in minutes (7 days = 10080 minutes).
# Requires pnpm >= 10.
minimum-release-age=10080
