# Code review — `refactor/diff-and-tests`

Generated by the reviewer agent on 2026-05-31 after the multi-agent
build that added file upload, change navigation, sidebar, footer,
within-note diff, accessibility, responsive, real-world fixtures, and
the bun→pnpm+vitest migration.

**Verdict:** FAIL → fixes applied (see commit history for resolution
of each item below).

**Summary at review time:** Blocking: 5 · Should-fix: 9 · Nice-to-have: 11.

## Critical (blocked merge) — all RESOLVED

1. `tests/e2e/features.spec.ts:489` — asserted a "Bun" footer link that
   no longer exists after migrating to pnpm/Vite. Renamed test and
   switched assertion to `/vite/i`.
2. `src/tests/diffXML.integration.test.ts:243` — `expect(size >= 0)` is
   trivially true. Changed to `>= 1` (Piano vs Violin in the fixtures
   guarantees a partLists entry).
3. `tests/e2e/library-export.spec.ts:25` — `execSync("bun run build:lib")`
   broke under pnpm-only environments. Switched to `pnpm run build:lib`.
4. `src/bootstrap/git-diff-page.ts:133` — `hasChanges` skipped
   `partLists.size`, and the render loop never emitted `partListHunks`.
   Both fixed; partList-only diffs now render.
5. `src/bootstrap/git-diff-page.ts:192` — `as unknown as { value: … }`
   bypassed type safety. `diffSettingsEl` parameter now typed as
   `DiffSettings`; the setter is reachable normally. Call site in
   `src/main.ts:83` also updated to use the typed selector.

## Major (deferrable but real)

| # | Location | Status |
|---|----------|--------|
| 1 | `src/main.ts:164-167` dead `prefers-color-scheme` listener | RESOLVED — listener removed |
| 2 | `src/main.ts:589-590` magic indices `[2]`/`[0]` into glob result | RESOLVED — `findScore(id)` lookup |
| 3 | `src/main.ts:141-142` `scoreLoaderEl` non-null assertions outside null guard | DEFERRED — low risk; covered if scoreLoader template renders correctly |
| 4 | `src/bootstrap/notation-pipeline.ts:179,184` `toolkit!` after null-handling `renderNotation` | DEFERRED — `reloadScore` callers in `main.ts` already ensure non-null toolkit |
| 5 | `src/main.ts:272-287` premature factory wrappers `makeModelUpdateCb`/`makeFilenameCb` | DEFERRED — cosmetic, not behavioural |
| 6 | `src/components/diffSettings/index.ts:56-58` commented-out type fields | DEFERRED |
| 7 | `src/main.ts:171` `rerenderScore2Background` missing `enrichOverlays` (a11y regression) | RESOLVED |
| 8 | `src/components/pages/index.ts:52,145` orphan TODOs | DEFERRED |
| 9 | `package-lock.json` tracked alongside `pnpm-lock.yaml` | RESOLVED — removed, added to .gitignore |

## Minor (nice-to-have)

1-3. `bun test` / `bun run` references in test/comment files — RESOLVED.
4. `xMLToCompare` → `xmlToCompare` rename — deferred (15 call sites).
5. `escapeHTML` duplicated in two modules — deferred (extract later).
6. ESLint directive in a Biome project — deferred.
7. Magic number `1700` for overlay focus duration — deferred.
8. `SplitRow` type hoist — deferred.
9. `diffXML` function 126 LOC — deferred (refactor candidate).
10. `diffSettings/index.ts` 413 LOC — deferred (acceptable for Shadow DOM web component).
11. README test count "61" → 136 — already fixed by documenter.

## Modern-practices compliance

All PASS except:
- `as unknown as` in `git-diff-page.ts:192` — RESOLVED.
- `main.ts` LOC > 200 plan target — still 615; deferred (decomposition would be its own commit).

## Security

All vectors PASS. Notable strength: every user-XML path is escaped
before reaching `innerHTML` via `escapeHTML`. `DOMParser` runs in
`application/xml` mode (no script execution). `.mxl` decompression
output is treated as untrusted text.

## Architecture notes

Defer: extracting `view-switch.ts` / `event-wiring.ts` from `main.ts`,
extracting `diffElementList` helper from `diffXML.ts`, moving
`diffSettings` inline CSS to a `?inline` import.

Keep as-is: `WeakMap` tooltip cache, `NotationState` context object,
`escapeHTML` consistency, CI workflow structure, fixture smoke suite,
`fakeToolkit` test pattern, `MonacoCallbacks` inversion.
